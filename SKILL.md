---
name: "customer-business-review"
version: "2.5.1"
description: "生成客户飞书整体使用情况回顾与数据洞察画板。用户提到回访、BR、Business Review、客户复盘、使用回顾或主租户近 180 天分析等相近意图时自动调用。"
---

# 飞书整体使用情况回顾

以客户主租户数据为起点，生成近 180 天飞书整体使用情况回顾和一张客户可见数据洞察画板。默认使用中文，强调数据完整、口径清晰、结构稳定和客户可读。

## 调用边界

- 当用户提到回访、BR、Business Review、客户复盘、使用回顾、飞书使用分析或数据洞察画板等相近意图时自动执行，无需用户明确点名本 Skill。
- 创建、评审或测试本 Skill 时不得查询真实客户；只做静态校验或使用明确标注的虚构数据。
- 运行时只读查询，不写回 C360，不自动向客户发消息，不自动分享文档。
- 当前调用者视为 CSM；文档与画板创建在其个人云盘个人空间。
- **交付到“飞书整体使用情况回顾”即结束。** 不生成案例、产品速递、服务计划、场景共创、内外双版本、交互卡片、一键定稿或会后自动更新。

## 输入

1. `customer_name` 或 `tenant_fcode` 至少提供一项；两者都有时交叉校验。
2. 可选 `review_reason`：仅用于理解回顾背景，不进入数据画板。
3. 可选 `review_month`：计划回访月份；缺失时使用当前月份。

先执行 [环境检测、安装与恢复](references/bootstrap-and-recovery.md) 和 [运行能力检测与用户提醒](references/runtime-capability-and-notification.md)。若未安装 `lark-c360`，自动安装并验证；不得仅告知用户“缺少工具”后停止。飞书权限、浏览器能力和 Aeolus 登录方式必须在启动时一次性预检。

若输入 F 码，先精确反查租户和所属客户，再查询该客户下全部租户并按 DAU 验证主租户。租户显示名与客户正式名称分开保存。客户无法唯一匹配时，要求 CSM 补充或选择，并停止其他动作。

## 强制执行顺序

严格遵循 [执行流程](references/workflow.md)、[数据源与工具路由](references/tool-routing.md)、[运行能力检测与用户提醒](references/runtime-capability-and-notification.md)、[Aeolus 浏览器自动化手册](references/aeolus-browser-runbook.md) 和 [参考 BR 与数据完整性门禁](references/reference-br-and-completeness.md)：

1. 在 C360 唯一匹配客户，取得关联租户、DAU、主租户与 F 码。
2. 以 DAU 最高的关联租户作为唯一主租户；其他租户不得参与汇总、对比或归因。
3. 将主租户 **F 码**回填到指定 Aeolus 看板。以 Aeolus 最近可用数据日为结束日计算滚动 180 天，再将同一日期范围用于 C360。不得用客户名或租户名替代 F 码，不得跳过回填。
4. 只有上述依赖成功后，补齐 C360 七模块固定字段和 Aeolus 180 天累计/日均指标。
5. 建立 coverage matrix，按七模块组合分析全部有效指标。
6. 生成一张数据洞察画板和一份“飞书整体使用情况回顾”文档。
7. 回读画板和文档，确认数据、结构、口径和视觉无误后结束。

阻塞条件：客户无法唯一匹配；主租户或 F 码缺失；F 码无法回填 Aeolus 并启动查询。任一发生时不得生成 BR。

## 数据与分析

按照 [数据与洞见规则](references/data-and-insights.md) 执行，以下规则不可降级：

- C360 与 Aeolus 同等重要，共同构成核心数据底座。
- 无浏览器能力时，允许用户提供 Aeolus CSV/XLSX、完整截图或已确认指标表；未取得 Aeolus 数据时只能输出预分析，不得生成正式 BR。
- 当前周期是以 Aeolus 最近可用数据日为结束日的滚动 180 天；同比周期是紧邻的前一个 180 天。C360 与 Aeolus 的当前周期起止日期必须完全一致。
- 后台分析全部可用指标，画板按模块只选 3～5 个关键指标。回顾正文必须展示所有有效核心指标和完整模块数据，不受该数量限制。
- 生成回顾文档前必须建立 coverage matrix，并按 [参考 BR 与数据完整性门禁](references/reference-br-and-completeness.md) 校验；任一有效核心字段未展示时不得宣称完成。
- 画板默认遵循七个独立一级模块：即时协同、会议协同、内容沉淀、多维表格、知识管理、AI 赋能、服务台。知识管理不得并入 AI 赋能，内容沉淀不得替代知识管理。
- 七个序号模块必须使用单列通栏布局，每个模块独占一行，不使用两列并排卡片。
- 模块标题统一采用 `01｜统一协同底座、工作入口稳定` 格式，序号后必须使用全角分隔符 `｜`。
- 禁止使用白色圆角方块包裹单字或缩写作为图标；优先使用无底框、简洁的线性 SVG 矢量图标，也可完全不使用图标。
- 每个模块的数据区必须使用 5 等分固定栅格；所有行复用相同的 5 个列中心点。
- 指标名称和指标值必须在各自列内水平居中，并共享同一中心轴。
- 单位写入指标名称，例如 `总会议时长（万分钟）`；数值区只保留纯数字，避免单位破坏居中对齐。
- 时间范围必须写 `近 180 天`，禁止将 180 天改写成“半年”“近半年”或“6 个月”。
- 每个模块的洞见必须至少包含一个可复核的数据关系，如日均值、单次均值、渗透率差、DAU 占比或 DAU/WAU。
- 禁止使用“高效流转”“价值显著”“持续提升”等没有数据关系支持的口号替代洞见。
- 洞见计算可在后台使用原始值和派生值，但画板与正文的洞见句不得重复上方已展示的指标或数值。
- 洞见区只写由数据关系推导出的结构判断、阶段判断、覆盖差距或使用特征。
- 必须形成“指标组合 → 客观事实 → 业务含义”的组合洞见，不得逐项复述指标。
- 只写客观事实和谨慎的业务含义，不输出原因假设。
- AI 消耗必须分析规模、趋势、渗透、人均、集中度和场景；无有效数据则隐藏模块。

运行时按 [数据源与工具路由](references/tool-routing.md) 调用现有 Skill。不得用通用搜索或自造请求替代 C360、Aeolus、飞书文档和画板的规定入口。

## 交付物

按照 [交付物与生命周期](references/deliverables-and-lifecycle.md) 创建：

- **单一数据洞察画板**：七模块摘要、统计数据、客观洞见和口径说明。
- **飞书整体使用情况回顾文档**：合规说明、核心结论、同一张画板、核心指标总览、七模块完整数据与客观判断。

文档命名：`客户名称｜飞书整体使用情况回顾｜YYYY-MM`。

## 异常与降级

- C360 或 Aeolus 局部缺失：仅当两套核心数据源均已成功启动且返回部分有效数据时继续；在文档口径说明中标记缺失范围，不据缺失数据下结论。
- 模块无有效数据：隐藏模块，不展示空状态。
- 同比基期无数据：只展示当前值，不展示同比。
- 任何无数据或无权限的情况：明确记录缺口，禁止编造。

## 完成检查

交付前逐项确认：

- 已唯一匹配客户，且只分析 DAU 最高主租户。
- 已使用主租户 F 码回填 Aeolus，而非客户名或租户名。
- C360 与 Aeolus 当前 180 天日期完全一致，同比为紧邻前 180 天。
- 洞见来自指标组合，事实与原因假设分离，每条画板洞见不超过 50 字。
- 只有一张总画板，且画板不含归因、内部资料或风险。
- 文档仅包含“飞书整体使用情况回顾”，没有案例、服务计划或其他后续章节。
- 文档与画板引用同一组数据和口径。
- 未写回 C360，未编造数据、对标或因果关系。
